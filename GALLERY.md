# 갤러리 사용 방법

## 사진첩 등록·수정

- 관리자는 사진을 여러 번 추가해 선택할 수 있습니다. 기존 사진과 신규 사진을 합해 최소 1장, 최대 50장입니다.
- 사진 아래의 화살표 버튼으로 순서를 바꿉니다. 새 사진을 기존 사진 사이에 배치할 수도 있습니다.
- **대표로 선택**을 누르면 목록에 표시할 사진이 선택됩니다. 사진 순서와는 별개로 유지됩니다.
- 기존 사진의 **삭제**는 저장 전까지 예약 상태이며 **삭제 취소**로 되돌릴 수 있습니다. 저장을 완료하면 삭제가 반영됩니다.
- 신규 사진을 선택 목록에서 삭제하면 해당 파일은 업로드하지 않습니다.
- 대표 사진을 삭제하면 남은 첫 사진이 대표 사진으로 선택됩니다.
- 저장 중에는 진행 상태를 표시하며, 오류가 발생하면 입력과 사진 선택을 유지합니다.

## 사진 보기

갤러리 목록, 홈페이지의 갤러리 미리보기, 관리자 목록에서 같은 대표 사진을 표시합니다. 이전 사진첩은 대표 사진을 따로 지정하지 않았어도 첫 사진을 표시합니다.

상세 화면에서 사진을 누르면 확대 보기가 열립니다. 이전·다음 버튼, 키보드 방향키, 모바일 좌우 넘기기를 지원합니다. 현재 사진 번호와 전체 장수를 표시하며 Esc 또는 닫기 버튼으로 나갈 수 있습니다.

## 적용 범위

사진 정보에 `sort_order`, `cover_photo` 컬럼이 추가됩니다. 현재 프로젝트의 JPA 설정에 따라 애플리케이션 재시작 시 반영되며, 기존 사진에는 기본값이 적용됩니다. 운영 DB 변경 명령이나 배포는 실행하지 않았습니다.

공통 파일 저장 서비스·보안·인증서 설정, Toast UI와 사용자 작업 중인 삭제 파일은 변경하지 않았습니다. 갤러리 전용 화면과 사진 관리 로직을 추가했으며, 기존 관리자 권한 규칙을 사용합니다.

## 검증

```text
gradlew.bat test --tests '*GalleryIntegrationTests' --tests 'com.SeochangChurchApplicationTests' --offline --no-daemon
node --test src/test/js/gallery.test.cjs
```

H2에서 순서·대표 사진 저장과 재조회, 삭제 후 첨부 매핑 제거, 다른 사진첩 참조 차단, 최소·최대 장수, 관리자 권한, 화면 렌더링을 검증합니다. 파일 저장은 테스트 대역을 사용하여 실제 업로드 파일을 변경하지 않습니다. JavaScript 테스트는 신규 사진의 전송 순서, 대표 사진 매핑과 좌우 넘기기를 검증합니다. 실제 브라우저의 터치 동작과 운영 PostgreSQL은 별도 확인이 필요합니다.
